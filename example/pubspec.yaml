name: flutter_upshot_plugin_example
description: Demonstrates how to use the flutter_upshot_plugin plugin.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.12.0 <3.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  showcaseview: ^2.0.0+1
  flutter_upshot_plugin:
    # When depending on this package from a real application you should use:
    #   flutter_upshot_plugin: ^x.y.z
    # See https://dart.dev/tools/pub/dependencies#version-constraints
    # The example app is bundled with the plugin so we use a path dependency on
    # the parent directory to use the current plugin's version.
    path: ../

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^1.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

dependency_overrides:
  meta: ^1.7.0
  flutter_svg: 0.22.0
# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
     - assets/UpshotPollTheme.json
     - assets/UpshotRatingTheme.json
     - assets/UpshotSurveyTheme.json
     - assets/UpshotTriviaTheme.json     
     - assets/new_tutorial_json.json
     - assets/images/avg_active.png
     - assets/images/avg_in_active.png
     - assets/images/bad_active.png
     - assets/images/bad_in_active.png
     - assets/images/happy_active.png
     - assets/images/happy_in_active.png
     - assets/images/buttonBg.png
     - assets/images/check_def.png
     - assets/images/check_sel.png
     - assets/images/close.png
     - assets/images/dislike_active.png
     - assets/images/dislike_in_active.png
     - assets/images/like_active.png     
     - assets/images/like_in_active.png
     - assets/images/close.png
     - assets/images/radio_def.png
     - assets/images/radio_sel.png
     - assets/images/check_def.png
     - assets/images/ratingBG.png
     - assets/images/slider_max1.jpg
     - assets/images/slider_min1.jpg
     - assets/images/slider_thumb.jpg
     - assets/images/star_active.png
     - assets/images/star_in_active.png
     - assets/images/surveyBG.png
     - assets/images/triviaBG.jpg
     - assets/images/unhappy_active.png
     - assets/images/unhappy_in_active.png
     - assets/images/upshot.png
     - assets/images/veryhappy_active.png
     - assets/images/veryhappy_in_active.png
     
  fonts:
     - family: PFHandbookPro
       fonts:
         - asset: fonts/PFHandbookPro-Regular.ttf
           weight: 500
         - asset: fonts/PFHandbookPro-Medium.ttf
           weight: 800
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
